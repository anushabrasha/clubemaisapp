import { PtQrFormatPipe } from './pt-qr-format.pipe';

describe('PtQrFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PtQrFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
